---
name: Bug report
about: Something does not work as expected
labels: bug
---

**What happened, and what did you expect?**

**Steps to reproduce**
1.
2.

**Your system**
- Ubuntu version (`lsb_release -ds`):
- GNOME Shell version (`gnome-shell --version`):
- Wayland or X11 (`echo $XDG_SESSION_TYPE`):
- Shell theme and panel style (Match the system theme / Windows 11):
- App you were pasting into:

**Log** (`journalctl -b -o cat /usr/bin/gnome-shell | grep -i winv`)
```
```
